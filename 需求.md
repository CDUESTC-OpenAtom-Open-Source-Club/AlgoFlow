# AlgoFlow（算法随想）项目需求规格说明书

> 文档版本：2.1（开发基线确认版）  
> 更新日期：2026-08-20  
> 目标平台：OpenHarmony 手机端 + 浏览器桌面工作台  
> 赛事依据：[2026 开源鸿蒙大学生创新大赛·赛道三](https://competition.gitcode.com/competition/2074797483593228289/intro)

## 一、项目定位

### 1.1 一句话定义

AlgoFlow 是一个面向算法学习者和开发者的多端思路验证代码工作台：用户可在手机上随时记录、阅读和修改代码，在电脑上继续编辑；AI 将用户当前表达忠实整理为伪代码或短代码片段，帮助用户快速检查思路是否完整、可实现，而不是擅自替换成标准答案。

### 1.2 核心价值

1. **随时捕获**：手机端快速记录脑中的算法步骤、边界条件和临时代码。
2. **跨端继续**：草稿、代码、AI 结果和复写版本在手机与电脑间同步。
3. **忠实转换**：AI 默认只结构化用户已经表达的思路，不补出隐藏的正确解法。
4. **快速验证**：通过假设列表、缺失信息、风险标记、手工样例推演和可选的受控执行，判断当前思路是否值得继续。
5. **学习自主性**：用户可隐藏 AI 结果，先独立复写，再进行差异对比和复盘。

### 1.3 非目标

- 首版不建设完整云端 IDE、在线评测平台或大型题库。
- 首版不在手机本地执行任意用户代码。
- 默认的“忠实转换”不承诺生成正确解法，也不允许静默修正用户算法。
- 服务卡片不是完整编辑器，只是快速记录、查看状态和回到应用的入口。
- 普通 HTTP/WebSocket 云同步是产品基础能力，不包装成 OpenHarmony 分布式特色。

## 二、平台与跨端决策

### 2.0 已确认开发基线

以下信息来自已创建并成功构建的 `C:\study\AlgoFlow` 工程，三名成员本地环境必须保持一致：

| 项目 | 当前值 | 备注 |
| --- | --- | --- |
| DevEco Studio | 6.0.0 Release，构建 6.0.0.858 | 统一版本，不使用 Beta/Preview |
| OpenHarmony SDK | API Version 20 | SDK 组件版本 6.0.0.47 |
| 已安装组件 | ArkTS、JS、Previewer、Toolchains | Native C++ 暂不安装 |
| 工程模型 | Stage | `entry` 模块，ArkTS + ArkUI |
| 目标设备 | Phone | 其他设备类型按需求另行验证 |
| `compatibleSdkVersion` | `6.0.0(20)` | 以工程配置为准 |
| `targetSdkVersion` | `6.0.0(20)` | 以工程配置为准 |
| 工程 `runtimeOS` | `HarmonyOS` | 当前模板实际生成值，不能宣称已验证纯 OpenHarmony 运行目标 |
| 构建状态 | HAP Debug 构建成功 | 尚未配置正式签名 |

SDK 管理器显示“OpenHarmony SDK”只能证明当前使用的 SDK 分发包；工程是否支持某项纯 OpenHarmony 能力，必须以 API 文档、编译结果和目标设备验证为准。不得手工修改 `runtimeOS` 绕过工具链判断。

### 2.1 客户端形态

| 客户端 | 首版技术方向 | 主要职责 |
| --- | --- | --- |
| OpenHarmony 手机端 | ArkTS + ArkUI 原生应用 | 快速记录、代码阅读/短编辑、AI 交互、复写对比、服务卡片 |
| 电脑端 | 响应式 Web 工作台，浏览器直接使用 | 长文本和代码编辑、版本对比、批量整理、同步状态查看 |
| 云端 | 华为云部署的同步 API、AI Gateway 和数据库 | 账号、同步、冲突控制、模型适配、审计与限流 |

电脑端不要求用户拥有鸿蒙 PC。Windows、macOS 和 Linux 均通过现代浏览器访问，降低三人团队的交付和维护成本。

### 2.2 白皮书采用结论

《跨平台框架白皮书 V1.0》对本项目有参考价值，但不直接决定框架。项目采用其“按 UI 层、业务逻辑层、平台能力层分层选型”的方法：

- OpenHarmony 服务卡片、生命周期、权限和设备能力使用原生实现。
- 手机与电脑共享数据协议、领域枚举、AI 输出 Schema 和同步规则，不强求共享 UI 代码。
- MVP 不引入 CMP、Flutter 或 React Native，避免在比赛周期内承担 OpenHarmony 社区适配、插件兼容和双工具链维护风险。
- 若 Web 桌面端无法满足离线、性能或交互需求，再以可运行原型、插件覆盖率、包体积和团队学习成本为依据评估 CMP/Flutter；不得仅因“代码复用率高”直接迁移。

### 2.3 OpenHarmony 特色

首个必须完成的特色能力为**服务卡片**，用于：

- 一键新建思路；
- 展示最近草稿或待复写任务；
- 展示最后同步状态；
- 点击后跳转到应用对应草稿。

分布式任务接续、设备间分享或协同能力属于 P2 验证项。只有在目标 OpenHarmony SDK、两台独立设备实例和官方 API 均验证通过后，才能作为赛事特色宣传。两台华为云手机可以用于普通账号云同步测试；能否验证 OpenHarmony 分布式能力取决于云手机镜像、网络发现、账号和系统能力，不能预先视为可用。

## 三、用户与场景

### 3.1 目标用户

- 算法竞赛和数据结构课程学习者。
- 需要在通勤、排队等碎片时间记录技术思路的开发者。
- 希望先验证思路、再决定是否进入完整编码的人。

### 3.2 核心场景

#### 场景 A：手机捕获，电脑继续

用户在手机输入“先按右端点排序，再依次选择不冲突区间”，保存后在电脑浏览器打开同一草稿，补充 C++ 片段和测试样例。同步不得覆盖任一端尚未提交的修改。

#### 场景 B：忠实转换，不泄露答案

用户的想法存在遗漏。AI 仍按原思路生成伪代码，列出“未说明相等端点如何处理”等缺失信息，但不自动补成正确贪心证明或替换算法。

#### 场景 C：隐藏与复写

用户让 AI 生成短代码片段后将其隐藏，自己独立复写；完成后查看结构差异、遗漏边界和复杂度差异。AI 版本与用户版本必须分别保存。

#### 场景 D：显式请求提示或解题

用户主动切换到提示或完整解题模式。界面必须清楚显示当前模式，生成结果不得覆盖忠实转换记录。

## 四、AI 能力与行为契约

### 4.1 四种模式

| 模式 | 默认 | 允许内容 | 禁止行为 |
| --- | --- | --- | --- |
| 忠实转换 | 是 | 用户思路对应的伪代码、短代码、假设、缺失信息、风险 | 静默引入新算法步骤或改成标准答案 |
| 可行性分析 | 否 | 边界、复杂度、反例方向、需要验证的条件 | 未经说明直接重写用户方案 |
| 渐进提示 | 否 | 从提问到方向、再到带空缺骨架的分级提示 | 默认直接给完整答案 |
| 完整解题 | 否 | 用户明确请求后的完整思路、证明和参考代码 | 与忠实转换结果混存或冒充用户原思路 |

### 4.2 忠实转换输出

```json
{
  "mode": "faithful_transform",
  "pseudocode": [
    {
      "step": "string",
      "source_refs": ["idea_segment_1"]
    }
  ],
  "code_snippet": "string or null",
  "assumptions": ["string"],
  "missing_information": ["string"],
  "risk_flags": ["string"],
  "added_algorithm_steps": []
}
```

每个生成步骤必须关联用户输入片段。没有 `source_refs` 的算法步骤属于 AI 新增内容：忠实转换模式必须阻止保存，或放入明确隔离的“AI 建议”区，绝不能混入用户原思路。

### 4.3 短代码片段边界

- 首个重点语言为 C++，后续再增加语言。
- 片段用于表达当前局部思路，不默认生成完整项目、完整题解或可直接提交的竞赛答案。
- 用户可以编辑 AI 片段，也可以隐藏后新建自己的复写版本。
- AI 结果必须标注模式、时间、供应商/模型标识、规则版本和来源草稿版本。
- 模型不可用时，用户仍可手工编辑、同步和查看历史。

### 4.4 模型服务

- UI 和领域层不得绑定某一家模型供应商。
- 默认模型由团队后续指定；架构支持用户配置自己的 API。
- 未配置模型时 AI 功能显示为未启用，不伪造本地结果。
- 选择外部模型 API 后，发送给该服务的内容属于向用户所选第三方传输，后续隐私说明必须如实披露。
- 客户端不得保存团队生产密钥；团队密钥由华为云 AI Gateway 保存。用户自带密钥优先使用系统安全存储或服务端加密托管，具体方案需单独评审。

## 五、范围与优先级

### 5.1 MVP：比赛主交付

#### A. 工作区与草稿

- 创建、编辑、查看、归档和删除草稿。
- 草稿包含题目/主题、用户思路、语言、代码文档、AI 产物、复写版本和同步状态。
- 手机支持代码阅读和短片段编辑；电脑端提供更适合键鼠的大编辑区。
- 本地离线可编辑，恢复网络后进入同步队列。

#### B. 手机电脑同步

- 同一账号在手机和电脑查看同一工作区。
- 每次写入携带实体版本号和客户端操作 ID，服务端执行幂等处理和乐观并发控制。
- 无冲突时自动同步；并发修改时保留双方版本并要求用户选择或合并，不采用静默最后写入覆盖。
- 展示 `仅本地`、`同步中`、`已同步`、`有冲突`、`同步失败` 状态。

#### C. AI 忠实转换

- 支持自然语言转伪代码和 C++ 短代码片段。
- 实现来源映射、结构化解析、长度限制和新增步骤检查。
- 至少准备 40 条契约测试，覆盖正确映射、错误思路、信息不足、提示注入、越权补解和格式异常。

#### D. 隐藏、复写与对比

- AI 产物可隐藏，不影响用户查看原始输入。
- 用户复写生成独立版本，不覆盖 AI 版本。
- 对比至少展示步骤对应、用户新增/遗漏内容和风险标记；不得用“正确率”伪装未经证明的评价。

#### E. 可行性快速检查

- 支持复杂度预估、边界条件清单和手工样例逐步推演。
- 结果必须区分“已验证”“推测”“待补信息”。
- 首版不在手机执行任意代码；受控云端编译/执行只有通过隔离和资源限制评审后进入 P1。

#### F. 服务卡片

- 完成快捷记录、最近草稿、待复写提醒、同步状态和应用跳转中的至少三项。
- 卡片信息使用最小必要数据，不展示完整代码或敏感内容。
- 在可用 OpenHarmony 云手机或设备上完成刷新、跳转和异常状态演示。

### 5.2 P1：增强快速验证

- 隔离的云端 C++ 编译/运行：固定镜像、CPU/内存/时间限制、禁网、临时文件和任务回收。
- 三级渐进提示与显式完整解题模式。
- 版本差异视图、基础语法高亮和常用代码模板。
- 语音输入，仅在 OpenHarmony SDK、麦克风权限和云手机能力验证后启用。

### 5.3 P2：验证后再决定

- OpenHarmony 分布式任务接续或设备分享。
- CMP、Flutter 或其他跨平台客户端框架原型。
- 桌面离线安装包、VS Code 插件、多人实时协作。
- 端侧模型、碰一碰、实况窗和大规模在线评测。

## 六、数据与同步需求

### 6.1 关键实体

| 实体 | 说明 |
| --- | --- |
| Workspace | 用户工作区及同步游标 |
| Draft | 题目/主题、当前思路、语言、状态 |
| IdeaSegment | 可被 AI 引用的用户思路片段 |
| CodeDocument | 用户代码、AI 代码或复写代码的独立版本 |
| AIArtifact | 模式、输出、来源映射、模型与规则版本 |
| SyncOperation | 客户端操作 ID、实体版本、操作类型和时间 |
| ConflictRecord | 并发版本、冲突字段和解决结果 |

### 6.2 同步原则

- 本地数据库是离线工作副本，云端是跨端同步的协调端，不是唯一可编辑入口。
- 删除使用可恢复的软删除/墓碑记录，确保另一端不会把已删除内容重新上传。
- 网络重试必须幂等，避免重复草稿和重复 AI 记录。
- 冲突不可静默丢数据；首版允许生成冲突副本，暂不实现实时协同编辑算法。
- 普通云同步和 OpenHarmony 分布式接续使用不同适配器、不同验收标准和不同宣传表述。

## 七、非功能与合规需求

### 7.1 可用性

- 手机核心操作单手可达，长代码不挤压控制区。
- 电脑端支持键盘、复制粘贴、搜索、撤销/重做和稳定的编辑区尺寸。
- 页面必须明确当前设备、保存状态、同步状态和 AI 模式。
- 服务卡片失败不影响主应用使用。

### 7.2 可靠性与安全

- AI、同步和云端执行均设置超时、取消、重试上限、限流及清晰错误提示。
- 日志不记录完整用户思路、代码、API 密钥、签名、Token 或设备标识。
- 云端代码运行若未完成沙箱、禁网、配额和回收验证，不得上线。
- Apache License 2.0 是源代码许可证，不是隐私政策。隐私政策可后续编写，但上架前必须完成。

### 7.3 平台基线

- 三人统一 DevEco Studio 版本和 OpenHarmony SDK。
- SDK 选择以 DevEco Studio 当前可安装、稳定且满足服务卡片 API 的版本为准，并在工程初始化当天锁定。
- `compatibleSdkVersion`、`targetSdkVersion`、设备镜像、Form 能力和权限以工程配置及实际验证为事实来源。
- 华为专有 HarmonyOS Kit 不得写成 OpenHarmony 公共能力。

## 八、三人团队协作

| 成员 | 主责 | 备份职责 |
| --- | --- | --- |
| A：OpenHarmony 客户端 | ArkUI、手机编辑体验、服务卡片 | 基础产品验收 |
| B：Web 与同步 | 桌面工作台、同步协议、冲突处理 | 后端部署 |
| C：AI 与质量 | AI Gateway、输出契约、测试、发布材料 | OpenHarmony 构建 |

- GitHub 为唯一代码协作源，使用短生命周期功能分支和 Pull Request。
- 公共 Schema、同步协议、数据库迁移、权限、构建和 `AGENTS.md` 修改必须由另一人审查。
- 至少两人掌握 DevEco 构建、华为云部署和发布候选版本生成。
- 本地完成日常编码和快速测试；华为云用于共享服务、统一集成、云手机和发布前验证，不要求所有编码放到云端。

## 九、里程碑

| 阶段 | 目标 | 退出条件 |
| --- | --- | --- |
| 架构冻结 | SDK、数据契约、同步协议、AI 模式、服务卡片范围 | 三份文档一致；最小技术原型通过 |
| 单端闭环 | 手机端草稿、忠实转换、隐藏复写、离线存储 | 核心流程可重复演示 |
| 双端闭环 | Web 工作台、账号、双向同步、冲突副本 | 手机与电脑连续编辑不丢数据 |
| 鸿蒙特色 | 服务卡片刷新与跳转 | 目标设备环境稳定演示并录像 |
| 发布候选 | 权限、隐私、签名、回归、赛事材料 | 应用市场送审和赛事提交材料齐备 |

赛事具体日期仍以官方页面为准；开始开发后应按实际启动日期重新换算周计划，避免沿用过期日历。

## 十、完成定义

功能只有同时满足以下条件才可标记为完成：

1. 代码已合并并经另一名成员审查。
2. 对应客户端或服务实际构建通过。
3. 正常、离线、超时、冲突或权限拒绝路径已验证。
4. AI 功能通过模式隔离和来源映射测试。
5. 平台能力在声明的 SDK 与设备环境中验证，有可重复步骤和证据。
6. 公共契约、迁移、隐私影响和三份项目文档保持一致。

## 十一、待确认事项

- OpenHarmony SDK/API 基线及云手机镜像。
- 华为开发者账号、应用创建、包名、正式签名和密钥负责人。
- 默认 AI 模型供应商、成本上限和用户自带 API 的密钥保存方案。
- Web 工作台域名、账号体系与华为云数据库选型。
- 服务卡片最终展示项和刷新策略。
- 隐私政策、数据保存期限及第三方模型披露文本。
